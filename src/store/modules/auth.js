const state = {}
const getters = {}
const mutations = {}
const actions = {
    login() {
        console.log('login');
    }
}

export default {
    namespaced: true,
    state,
    getters,
    mutations,
    actions
}